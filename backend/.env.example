# RepoPilot — copy to backend/.env and adjust

# Required in production
JWT_SECRET=change-me-to-a-long-random-string

# Frontend URL (CORS)
FRONTEND_URL=http://localhost:5173

# Database (default SQLite file in backend/)
DATABASE_URL=sqlite:///./repopilot.db

# LLM defaults
DEFAULT_LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434

# Optional server-side API keys (users can also paste keys in the Agent panel)
OPENROUTER_API_KEY=
CURSOR_API_KEY=
